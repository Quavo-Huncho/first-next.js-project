export default function Footer() {
  return (
    <div style={{display: 'flex', backgroundColor: 'black', color: 'green', paddingTop: '20px', paddingBottom: '20px', marginTop: '-10px', fontSize: '20px', gap:'560px'}}>
      <button style={{all:'unset', color:'inherit', cursor: 'pointer'}}>Comment</button>
      <button style={{all:'unset', color:'inherit', cursor: 'pointer'}}>Like</button>
      <button style={{all:'unset', color:'inherit', cursor: 'pointer'}}>Share</button>
    </div>
  )
}